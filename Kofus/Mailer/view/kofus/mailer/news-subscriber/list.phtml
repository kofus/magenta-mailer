<?php echo $this->nodeNavigation('SCB', 'list')->render() ?>

<?php if ($this->paginator->count()) : ?>
    <table class="table table-hover">
        <thead>
            <tr>
                <th>E-Mail</th>
            </tr>
        </thead>
        <tbody class="rowlink" data-link="row">
            <?php foreach ($this->paginator as $entity) : ?>
                <tr>
                    <td>
                        <a href="<?php echo $this->url('kofus_mailer', array('controller' => 'news-subscriber', 'action' => 'view', 'id' => $entity->getNodeId())) ?>">
	                        <?php echo $this->escapeHtml($entity->getEmailAddress()) ?>
	                    </a>
                    </td>
                </tr>
            <?php endforeach ?>
        </tbody>
    </table>
    <?php echo $this->paginationControl() ?>
<?php else : ?>
    <p>Keine Einträge.</p>
<?php endif ?>